# Blazor

This course covers component structure, forms, state, and JavaScript interop.

## Component-based UI

Blazor pages are built from reusable Razor components that can receive parameters, handle events, and compose other components.

## Course navigation

The left tree comes from `courses/blazor/toc.yml`. It does not include ASP.NET Core or data-access topics.

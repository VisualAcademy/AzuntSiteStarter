# JavaScript interop

JavaScript interop is useful when a browser API or existing JavaScript library is not directly available from .NET.

## Calling JavaScript

Keep interop calls behind small services so components stay focused on UI behavior.

## Cleanup

Dispose JavaScript module references and event handlers when a component no longer needs them.

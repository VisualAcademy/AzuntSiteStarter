# Entity Framework Core

This section covers `DbContext` lifetime and common query patterns.

## Topics

The local navigation comes from `ef-core/toc.yml` and does not include the SQL or repository sections.

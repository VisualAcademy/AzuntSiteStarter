# Data access

This course collects practical data-access topics for ASP.NET Core applications.

## Topics

The sample covers `DbContext`, querying, connection configuration, and repository boundaries.

## Local navigation

Pages in this folder use `courses/data-access/toc.yml`, so the course tree stays focused on data access.

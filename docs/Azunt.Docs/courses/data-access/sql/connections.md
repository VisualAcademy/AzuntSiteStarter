# Connections

Keep production connection strings outside source control and load them through configuration providers.

## Development configuration

User Secrets are convenient for local development.

## Production configuration

Use the deployment platform's secure configuration or secret store rather than committing credentials to the repository.

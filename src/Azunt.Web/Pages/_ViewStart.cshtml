@{
    // Razor Pages shares the exact MVC landing shell by default.
    // Pages can override this directly or through a nested _ViewStart.cshtml.
    Layout = "/Views/Shared/_PublicLayout.cshtml";
}
